{
  "name": "@tokenring-ai/checkpoint",
  "version": "0.2.0",
  "description": "State preservation service for pausing and resuming agent workflows.",
  "dependencies": {
    "@tokenring-ai/app": "workspace:*",
    "zod": "^4.3.6",
    "@tokenring-ai/agent": "workspace:*",
    "@tokenring-ai/lifecycle": "workspace:*",
    "@tokenring-ai/utility": "workspace:*",
    "@tokenring-ai/rpc": "workspace:*"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  },
  "type": "module"
}
